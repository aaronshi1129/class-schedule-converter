export const CONFIG = {
    // Set this to true if you want descriptions to be included in calendar events
    includeDescriptionInEvents: true,
    
    // Default semester length (in months) for date pickers
    defaultSemesterLength: 4,
    
    // Date format for displaying skipped dates
    dateDisplayFormat: { 
        month: 'short', 
        day: 'numeric', 
        year: 'numeric' 
    }
};